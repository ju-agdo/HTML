 -> HTML
  Linguagem de marcação de hypertexto usada
  para criação de páginas na web.
  Criada entre 1989 e 1990 para compartilhar
  pesdwuisas cientificas entre Tim Berners-Lee
  e seus colegas.
  Ficou populas junto com a internet.
-> O HTML usa Tags
-> A descrição em cima do texto constando como <!DOCTYPE html> 
significa que está informando para o browser que estamos usando
o HTML5.
-> A tag <head> </head>  no HTML é a parte da cabeça e a tag
<body> </body> é a parte do corpo, onde fica todo o conteúdo 
do site.
-> Documentação MDN da Mozilla, na parte de elementos tem
as tags que podemos usar.
-> Tags básicas do HTML
Temos o h1 que define o tamanho da fonte, temos do h1 que é
o maior até o h6 que é o menor
-> No HTML existem dois tipos de listas, as ordenadas e as 
não ordenadas.
Listas ordenadas: tem uma progressão como:
1 -Mercurio
2 -Venus
3 -Terra
4 -Marte
5 - ...
Mostra as proximidades dos planetas com o sol
Listas não ordenadas: não existe uma ordem entre as informações
exibidas
Jupiter
Saturno
MarteUrano
....
Por exemplo nesse caso só demonstra os planetas do nosso sistema
solar, sem ter uma ordem específica
-> Organizando nosso código com Divs e Span
A Div <div></div> serve para criar blocos no código e todas as
alterações que eu fizer, como por exemplo trocar a cor do fundo
vai ser alterado somente naquele bloco.
O Span <span></span> serve para grifar somente o campo da palavra
desejado, diferente do div que grifa toda a linha, o span grifa/preenche
o fundo somente da palavea que está dentro de span.
Para saber todos os atributos disponíveis no HTML posso ir na documentação do W3Schools

Documentação W3Schools


Forms são campos que recebem as entradas dos usuários (inputs)inputs é a tag de entrada

    Como fazer as validações no forms?

Existem as validações, que é para verificar se os campos foram devidamente preenchidos.
Por exemplo, no código montado em Exemplo_Forms.html se não preenchermos a caixinha (input)
e clicarmso em pesquisar, vai nos direcionar para o Google, conforme link inserido. 
Para tornarmos esse preenchimento obrigatório é necessário fazer uma validação, essa é realizada 
por meio do atributo required, o required é para obrigar o usuário a preencher um campo.
Na minha linha de <input type="text" id="q"... insero no final o atributo required>
<input type="text" id="q" name="q" placeholder="Nasa, SpaceX" required/>
Existe também a validação do campo do tito e-mail, se colocar na minha type="email" e no campo de pesquisa do
input colocar um nome qualquer, vai retornar um erro informando que um e-mail contém @. Da mesma forma se o meu 
type="number' e colocar algo diferente de número, vai dar erro, ainda é possível realizar uma condição, colocar um minímo 
ou máximo.
Para colocar a condição de min e max, sõ acrescentar no final da linha do input min="0" max="10">
Na W3Schools tem a documentação sobre validação(vai estar na paste de input attributes)
Todos os campos "quadradinhos" que preenchemos nos sites são os inputs
Os checkbooks para "ticar", a opção para enviar/adicionar arquivo tem nos inputs também.


  Novidades do HTML5
É a última versão lançada do HTML.

  Elementos semânticos do HTML5 (semantics)
É o estudo do significado das palavras e frases em um idioma




